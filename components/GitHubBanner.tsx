/** A custom credit banner. */
export default function GitHubBanner() {
  return (
    <div className="flex items-center justify-center rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 shadow-sm">
      <p className="text-xs font-medium text-gray-700">
        ဖီးနစ် (ထူးကြီး) မှ ပြုပြင်မွန်းမံထားသည်
      </p>
    </div>
  );
}
